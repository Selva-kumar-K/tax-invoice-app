import Form from "./../../../../components/Form";

export const metadata = {
  title: "Update - Invoices App",
};

export default function page({ title }) {
  return <Form title="Update Invoice" />;
}
